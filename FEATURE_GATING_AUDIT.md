# Feature Gating Audit — Page-by-Page / Surface-by-Surface

Evidence-based table of where UI, marketing, and backend enforcement agree or disagree. "Evidence" cites the exact file/quote found. Severity: **Critical** (customer-facing false promise / revenue risk) · **High** (contradicts documented backend decision) · **Medium** (architectural duplication risk, not yet proven wrong) · **Low** (cosmetic/naming only).

| Surface | Issue | Evidence | Expected | Actual | Severity | Files |
|---|---|---|---|---|---|---|
| Pricing page | Growth Engine shown as included in Growth/Business plans | `WhatIsIncluded.jsx` line 162: `<Feature label="Growth Engine (AI Retention)" starter={false} growth={true} business={true} />` vs seeder: `'growth_engine' => ['trial'=>'0','starter'=>'0','growth'=>'0','business'=>'0']` | Growth Engine should be marked as a paid add-on for all tiers, or a distinct add-on row, not a per-tier included/excluded feature | Marketing claims it's bundled in Growth/Business; backend blocks it on every plan by default | **Critical** | `resources\js\Pages\WhatIsIncluded.jsx`, `database\seeders\PlanFeatureMatrixSeeder.php` |
| Pricing page | Plan-tier naming collision with feature naming | `Marketing\Pricing.jsx` line 545 and `WhatIsIncluded.jsx` line 545 both label the Growth *tier* `"Growth Engine"` | Tier name and add-on feature name should be visually/textually distinct | Same string used for both a subscription tier and an unrelated add-on feature | Medium | `resources\js\Pages\Marketing\Pricing.jsx`, `resources\js\Pages\WhatIsIncluded.jsx` |
| Billing page (in-app) | Upgrade CTA falsely promises Growth Engine unlock | `Billing\Index.jsx` `FEATURE_UPGRADE_TARGET` line 43: `growth_engine: 'growth'` | Should route to an add-on purchase flow / show "contact sales" for add-on-only features, not imply plan upgrade unlocks it | Tells an existing customer upgrading to Growth will unlock Growth Engine — false per backend | **Critical** | `resources\js\Pages\Billing\Index.jsx` |
| Billing page (in-app) | Same false-promise pattern for WooCommerce | `Billing\Index.jsx` `FEATURE_UPGRADE_TARGET`: `woocommerce: 'growth'` vs seeder comment: *"WooCommerce is NOT included in any plan — it stays off until sold separately... never a plan default."* | Should not map WooCommerce to any base-plan upgrade target | Implies Growth plan includes WooCommerce sync | **Critical** | `resources\js\Pages\Billing\Index.jsx`, `database\seeders\PlanFeatureMatrixSeeder.php` |
| Upgrade modal (global component) | Fifth independent hardcoded feature-name/icon map | `UpgradeModal.jsx` line 84 (feature list mentions "Growth Engine (AI retention)"), line 119 (`growth_engine: { icon: '✨', label: 'Growth Engine' }`) | Should read feature labels/icons from one shared config, not redefine them per component | Independent hand-maintained copy, diverges silently if changed elsewhere | High | `resources\js\Components\UpgradeModal.jsx` |
| Navigation / sidebar | Not directly audited in this pass — no sidebar-specific entitlement-gating file was read line-by-line in the underlying research. | — | Sidebar items should hide/lock based on the same single entitlement source as route middleware | **Not confirmed either way — flagged as an audit gap, not asserted as broken.** | — | Follow-up needed |
| Module landing pages | Three different "locked feature" components used inconsistently across pages | See `UX_CONSISTENCY_AUDIT.md` for full detail | One shared lock/gate component with one copy voice | `PlanGate.jsx`, `FeatureLock.jsx`, `FeatureLockBadge.jsx` all coexist, reading from different data sources | High | `resources\js\Components\PlanGate.jsx`, `FeatureLock.jsx`, `FeatureLockBadge.jsx` |
| Settings / feature toggles | SuperAdmin plan editor keeps its own hardcoded copy of the full feature matrix | `resources\js\Pages\SuperAdmin\Plans\featureGroups.js` (~line 1748) — JSON literal duplicate of `PlanFeatureMatrixSeeder.php` | SuperAdmin editor should read current DB state directly, not a hardcoded default mirror, or at minimum the mirror must be kept byte-for-byte in sync by process | If seeder is edited without updating this file (or vice versa), SuperAdmin UI shows stale default/display state | High | `resources\js\Pages\SuperAdmin\Plans\featureGroups.js`, `database\seeders\PlanFeatureMatrixSeeder.php` |
| Trial experience | Trial plan limits (`sku_limit: 50`, `staff_limit: 2`, `locations: 1`) confirmed present in seeder; no separate contradiction found in marketing copy for trial specifically in the evidence gathered. | Seeder Group data | — | **No contradiction found** — noted for completeness, not a finding. | Low/None | `database\seeders\PlanFeatureMatrixSeeder.php` |
| Sign in / sign up pages | Not directly audited — no plan-entitlement logic expected on these pages and none was found referenced. | — | — | No finding | — | — |
| Landing / marketing pages (general) | "226+ features" claim with no locatable code-derived count | grep across `About.jsx`, `LandingPage.jsx` (x3), `Marketing\Shared\FeatureDemos.jsx` found the claim; no canonical feature-count registry/constant was found anywhere in the codebase to substantiate 226 | Marketing numeric claims should derive from a real feature catalog, or be documented as manually curated and periodically reconciled | Appears to be a hand-authored marketing number, unverifiable against code | Medium | `resources\js\Pages\Marketing\About.jsx`, `LandingPage.jsx`, `Marketing\Shared\FeatureDemos.jsx` — see `MARKETING_CONSISTENCY_AUDIT.md` |
| Landing / marketing pages | "40+ reports" claim | Cross-checked: seeder has 39 `report_*` keys + 4 aliased keys = 43; `routes\web.php` has 52 actual `/reports/*` GET routes | Should match a real count | Reasonably substantiated (43–52 depending on counting method) — **not a finding, confirms this specific claim is roughly accurate** | Low/None | `database\seeders\PlanFeatureMatrixSeeder.php`, `routes\web.php` |
| Emails | Not audited in this pass — no Mailable classes were read for entitlement-related copy. | — | — | **Not confirmed — audit gap.** | — | Follow-up needed |
| AppSumo / LTD mapping | LTD tiers correctly delegate to `PlanRepository::getLtdSnapshot()`, not a separate hardcoded map | `app\Http\Controllers\AppSumoController.php`; `PlanRepository` docblock references a documented prior bug fix ("Session-3 fix / VNQ-011 follow-up") where snapshotting used to silently drop `ltd`/`hosted_until` keys, now fixed | Should not duplicate entitlement logic | **This one is done correctly** — flagged as a positive example, not a problem. LTD tiers do inherit the same `growth_engine: '0'` default and same false-marketing-promise risk as base plans, however (see Critical rows above). | Low/None (design) but inherits Critical risk above | `app\Http\Controllers\AppSumoController.php`, `app\Services\PlanRepository.php` |
| API entitlement enforcement | `api_access` key exists (Business only: `1`, all others `0`) but no direct evidence was gathered confirming an API middleware actually checks this key on every API route (as opposed to only gating a settings toggle) | Seeder value found; enforcement point not directly traced to a specific `routes\api.php` middleware in the evidence gathered | API routes should be gated the same way web routes are, via `plan.feature:api_access` or equivalent | **Not confirmed — needs a direct trace of `routes\api.php` before asserting enforcement is correct or missing.** | Flagged as audit gap | `routes\api.php` (not yet traced), `database\seeders\PlanFeatureMatrixSeeder.php` |
| Backend middleware / policies / gates | Core chain is sound: `EnsurePlanFeature` used 52+ times, `PlanGate`/`PlanRepository` centralize logic, fail-closed default | Confirmed via direct code read of `app\Services\PlanRepository.php`, `app\Services\PlanGate.php`, `app\Http\Middleware\EnsurePlanFeature.php` | — | This is the one part of the system that is architecturally correct — the problems are in the layers that don't consult it. | Positive finding | `app\Services\PlanGate.php`, `PlanRepository.php`, `app\Http\Middleware\EnsurePlanFeature.php` |
| Backend | Confirmed real production gap, same-day fixed | Seeder comment dated 2026-08-07: 3 report keys (`discount_report`, `cash_flow_report`, `stock_valuation`) used by route middleware but never seeded, causing fail-closed lockout for ALL plans including Business/LTD-3 | Every key referenced by `plan.feature:` middleware must exist in the seeder | Confirmed this class of bug already occurred in production-adjacent code | **Critical (historical, now fixed)** — cited as proof the architecture needs a guard-rail (e.g. a CI check cross-referencing route middleware keys against seeder keys) | `database\seeders\PlanFeatureMatrixSeeder.php`, `routes\web.php` |
| Frontend entitlement read path | Two competing Inertia shared-prop namespaces | `usePlan.js` reads `usePage().props.plan.features[key]`; `PlanGate.jsx`/`FeatureLock.jsx` read `usePage().props.store.features[key]` | One canonical prop namespace, one hook, used everywhere | Two parallel namespaces exist; which a given page uses depends on what was copy-pasted | High | `resources\js\Hooks\usePlan.js`, `resources\js\Components\PlanGate.jsx`, `FeatureLock.jsx` |

## Audit gaps (explicitly not confirmed, listed so they aren't silently assumed)

- Sidebar/navigation entitlement-gating logic — not traced line by line.
- Email templates — not searched for entitlement copy.
- `routes\api.php` — API route-level `plan.feature:` enforcement not directly confirmed present or absent.
- Executive dashboard — not specifically located/read in this pass.
- Whether `PlanFeature` (DB model, distinct from `PlanLimit`) is consulted anywhere at runtime.

These should be closed out in a follow-up pass before treating this audit as fully exhaustive on every requested surface.
