# Remaining remediation — 11 September 2026

**Latest acceptance plan:** [ROAD-TO-100.md](ROAD-TO-100.md) incorporates Claude's subsequent round-4 work and newly checked evidence. This file preserves Codex's earlier installation/build/backend-run results; its skipped-test counts describe that earlier snapshot only.

This update supersedes the unfinished-dependency statements in REMEDIATION-STATUS.md and the older snapshot in RECHECK.md. No deployment or paid service purchase was performed.

## Work completed in this pass

- Resolved and installed patched PHP dependencies; Composer's locked dependency audit reports **zero advisories and zero abandoned packages**. The lockfile is compatible with the declared PHP 8.2 support; Symfony components previously requiring PHP 8.4 were resolved onto compatible patched 7.4 releases. Laravel remains on major version 12. Horizon's Unix-only pcntl/posix requirements were ignored only for installation on the Windows test machine; they were not removed from the packages. Production must satisfy its platform requirements.
- Added `endroid/qr-code` 5.1, matching the QR service's existing builder API. Corrected stale source documentation saying the library was not installed.
- Upgraded Station from Electron 28 to **44.3.0** and electron-builder 24 to **26.15.3**. Preserved the previously updated electron-updater 6.8.9.
- Removed obsolete Jimp and its vulnerable transitive dependencies. The local icon tool now uses the existing png2icons library directly, creates output directories, fails with a nonzero exit status on errors, and accepts an explicit output path. It generates desktop icons without overwriting the web application's separately maintained favicon.
- Made theme generation leave identical output untouched, fixing the observed Windows write-lock failure during production builds.
- Added regression checks for OTP global quota exhaustion and dropping expired, consumed or superseded mail before invoking its transport.
- Regenerated the stale system manifest through its generator. Isolated ledger-audit test reports and registry output under Laravel's disposable testing directory, preserving the command's normal output locations outside tests.

## Earlier fixes preserved and checked

Claude had made another round of changes after my earlier audit. These changes were already present when this pass began: required legacy terminal re-pairing, single-use pairing token claims, Windows pairing UI and submission, authenticated screenshot handling and stronger encryption, atomic OTP send-budget reservation, locked resends, and stale queued-mail checks. I preserved those changes rather than reimplementing them. The focused backend tests exercise authentication, tenant permissions, terminal enrollment and screenshots.

## Verification

| Check | Result |
|---|---|
| Web npm audit | Zero known vulnerabilities |
| Windows npm audit | Zero known vulnerabilities |
| Composer locked audit | Zero advisories; zero abandoned packages |
| Frontend tests | 103 passed in four files |
| Production web build | Passed client and SSR build, including the build command's preliminary checks |
| Focused initial backend run | 66 passed, four QR tests skipped because that process started before library installation completed |
| Broader backend run | 2,420 passed, 127 skipped, 44 incomplete; two failures were stale generated manifest / report-file write access, subsequently resolved and retested |
| Focused follow-up | 44 passed, zero errors/failures/skips: both previously failing audit/manifest checks, OTP (including new quota/stale-mail regressions), and actual QR PNG/SVG rendering |
| Audit-output isolation regression | Two audit tests passed after the final output-path change; both report and registry files were created under storage/framework/testing/ledger-truth |
| Windows packaging | Unsigned local `--win --dir --publish never` build passed |
| Packaged native-module smoke check | Executable reports Electron 44.3.0 / Node 24.20.0 and loads serialport successfully |
| Icon conversion | Generated a local ICO successfully with the replacement converter |
| Composer manifest | Valid and consistent with lockfile; existing wildcard-version warnings remain |

Raw audits and test/build logs are saved alongside this file under `fix-*-2026-09-11.*`. The full suite was not repeated after the focused fixes: the affected tests were rerun instead. Skipped/incomplete tests are not passes and remain coverage limitations. Backend tests use a separately initialized MariaDB instance bound to 127.0.0.1:33079 and database `security_fix_test`, with its data directory under scratch/security-fix-db-20260911. The test server is MariaDB 10.4.32 and PHP is 8.2.12, so production PHP 8.4/MariaDB compatibility still needs a staging check. Tests do not establish that production has been configured or deployed correctly. The original isolated exploit probe is historical and deliberately expects the old vulnerability; do not use it as a passing regression test.

## Remaining launch actions

1. **Email provider/domain access:** configure the sender, DNS, queue worker and scheduler; demonstrate signup/login codes arriving in real inboxes. The default shared allowance is 90 OTP sends/day, including resends and logins, not 90 accounts. Keep it within provider limits. Exhaustion deliberately refuses sending; do not disable required OTP as an automatic fallback.
2. **Production infrastructure access:** verify Cloudflare proxy/origin/TLS settings, rate limits, secret protection, document root, queue/scheduler health and off-site backups. Perform a staging restore drill. DDoS protection has not been live-verified.
3. **Private owner/account actions:** enroll the real owner's authenticator, secure recovery codes, supply Turnstile configuration and complete payment-provider approval if payments are launching.
4. **Windows hardware acceptance:** pair a new and legacy till, test printer/cash drawer/scanner/scale, disconnect/re-pair, offline behavior and updates on the installed application. The package is unsigned; code signing is a separate optional paid decision and has not been purchased.
5. **Business and release approval:** confirm accurate policy/contact/trial wording, review the final changes, deploy the updated lockfiles and code, run migrations and repeat staging smoke checks on the actual host. Optional paid services remain deferred as specified in REPORT.md.

Dependency updates, code changes and local checks in this pass required no new paid subscription. Zero advisory counts are a useful verification result, not a guarantee that the application is unhackable.
