One-line: sidebar row — the most-looked-at pixel in the product.

```jsx
<SidebarItem icon={<Icon/>} label="Dashboard" active/>
<SidebarItem icon={<Icon/>} label="Tasks" badge={12}/>
```

Active = `--vq-accent-quiet` wash + 3px mint left rule + weight 600. Hover changes background and colour ONLY — never a transform, never a scaling icon.
