One-line: the inventory-health / project-progress donut.

```jsx
<ProgressRing value={41} sublabel="Project ended"/>
<ProgressRing value={75} color="var(--vq-series-3)" size={104}/>
```

Rounded stroke caps and a 14px thickness — this is a chunky, friendly gauge, not a hairline. One per card.
