One-line: checkbox for row selection and multi-pick lists.

```jsx
<Checkbox label="Track batch & expiry" checked={on} onChange={setOn} />
```

8px radius (the system's smallest) so it reads as a control, not a pill.
