One-line: settings toggle — use for instant-apply state, never as a form field you must submit.

```jsx
<Switch label="Email me the month-end summary" checked={on} onChange={setOn} />
```

The knob rides `--vq-ease-spring` and overshoots ~6%; the track colour change is fast and flat.
