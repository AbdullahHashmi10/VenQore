One-line: the theme switch — top-right of the app top bar, bottom of the marketing nav.

```jsx
<ThemeToggle/>
```

Both themes are first-class: dark is selected, not inverted. The glyph rotates on `--vq-ease-spring`; nothing else moves.
