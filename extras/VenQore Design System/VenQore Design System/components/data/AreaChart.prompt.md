One-line: the trend chart on the dashboard (Purchases trend, Revenue analytics).

```jsx
<AreaChart data={[700,1400,2100,2400,2600,5200]} labels={["Mar","Apr","May","Jun","Jul","Aug"]}/>
```

Slot 1 (mint) unless the card already owns another series colour. Never a dual axis — two measures means two charts.
